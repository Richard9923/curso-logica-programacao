
const nomes = ['ana Julia', 'Caio vinicius', 'BIA silva'];

const upperCase = nomes.map((nome) => nome.toUpperCase());

const arrayNums = [1, 2, 3, 4];

const multiplicando = arrayNums.map((num) => {
    return num * 10;
})

console.log(multiplicando);